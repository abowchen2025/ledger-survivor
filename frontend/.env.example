# 複製為 frontend/.env 後依本機環境調整；.env 不進版控。兩個都是 Vite 建置期變數（VITE_ 前綴才會進 bundle）。

# 後端 API 位址，只到 host[:port]，不含 /api/v1（路徑由 src/api/schema.d.ts 的 paths 帶）。
# 寫 127.0.0.1 不寫 localhost：Node 會把 localhost 解析成 IPv6 ::1，uvicorn 預設只綁 IPv4（見 CLAUDE.md）。
# GitHub Pages 建置時由 GitHub Secret VITE_API_BASE_URL 注入 Railway 網址（https://xxx.up.railway.app）。
VITE_API_BASE_URL=http://127.0.0.1:8765

# REQ-AUTH-000 臨時 API 金鑰，所有 /api/v1 請求帶 X-API-Key 標頭。要與後端 .env 的 API_KEY 相同。
# 這是開發用值，不是機密。Pages 建置時由 GitHub Secret VITE_API_KEY 注入；
# 注意金鑰會嵌進前端 bundle，任何人開 devtools 都看得到——它擋的是隨機掃描，不是針對性攻擊。
VITE_API_KEY=dev-local-only-not-a-secret

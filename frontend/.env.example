# 複製為 frontend/.env 後依本機環境調整；.env 不進版控。都是 Vite 建置期變數（VITE_ 前綴才會進 bundle）。

# 後端 API 位址，只到 host[:port]，不含 /api/v1（路徑由 src/api/schema.d.ts 的 paths 帶）。
# 寫 127.0.0.1 不寫 localhost：Node 會把 localhost 解析成 IPv6 ::1，uvicorn 預設只綁 IPv4（見 CLAUDE.md）。
# GitHub Pages 建置時由 GitHub Secret VITE_API_BASE_URL 注入 Railway 網址（https://xxx.up.railway.app）。
VITE_API_BASE_URL=http://127.0.0.1:8765

# 本機開發用的 API 金鑰預設值（REQ-AUTH-000），要與後端 .env 的 API_KEY 相同。
# 只在 npm run dev（dev build）生效；production build 一律忽略，所以 Pages 產物裡沒有任何金鑰。
# 正式環境的金鑰由使用者在「設定」頁輸入一次、存瀏覽器 localStorage（優先於這個預設值），見 docs/adr/0007。
VITE_DEV_API_KEY=dev-local-only-not-a-secret
